# OnePost
  Run the OnePost
# STEP 1:
  $ cd OnePostServer
# STEP 2:
  $ python3 -m venv env<br />
  $ source env/bin/activate<br />
  $ pip install --upgrade pip<br />
  $ pip install -e .
# STEP 3:
  $ source env/bin/activate<br />
  $ cd onepost<br />
  $ ./OnePostRun
